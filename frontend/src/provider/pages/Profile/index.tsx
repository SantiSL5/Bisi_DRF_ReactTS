import { useUsers } from "../../hooks/useUsers";




const Profile = () => {
    const { user } = useUsers();

    return (
        <div className="adminView">

        </div>
    );
}

export default Profile;